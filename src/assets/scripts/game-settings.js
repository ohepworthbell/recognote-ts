import notes from './note_references';

/**
 *  Set game settings
 * 
 */
let settings = {
  nodes: 100,
  radius: 190,
  lineWidth: 10,
  maxOffset: 100,
  hardMode: false,
  instrument: 'midi'
};

export {notes, settings};
